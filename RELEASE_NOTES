R1.2.5-ued_llrf-1.1     04-02-2019  Kukhee Kim (khkim)
                - add PLL loops monitoring PVs (LO, Clock and All-Digital PLL loops)
                
R1.2.5-ued_llrf-1.0     03-15-2019  Kukhee Kim (khkim)
                - support 6 tiimeslot firmware for 360Hz operation


Branch out to ued_llrf,  03-15-2019 Kukhee Kim (khkim)
               - support 6 timeslot firmware for 360Hz operation

R1.2.5         05-30-2018     Thomas Kurty (tkurty)
               - Fix temperature sensor "Least Significant Byte" (LSB) register read

R1.2.4         05-25-2018     Thomas Kurty (tkurty)
               - Phase correction sign is flipped somewhere in firmware (v. 2018-05-10-13:53:18)
               - Flip sign again in KlystronLib as a workaround
                 - Negate sine term only, cosine unaffected by negative phase

R1.2.3         04-27-2018     Kukhee Kim (khkim)
               - Update API for latest firmware changes:
                 - Comment out packet counter register in SwRssiServer
                 - add msgNoDelay register to control the LCLS1 timing pattern delay.
               - Bump up to cpsw/framework R3.6.4

R1.2.2         02-26-2018     Thomas Kurty (tkurty)
               - Update AppTop and AppCore register names from new firmware (2018-01 and later)
               - Add Software Trigger register for debugging
               - Remove outdated yaml files (not used by this package)

R1.2.1         05-08-2017     Thomas Kurty (tkurty)
               - Revert incorrectly named Klystron variables back to Llrf

R1.2.0         05-01-2017     Thomas Kurty (tkurty)
               - Rename library from llrfLib to KlystronLib

R1.1.0         03-22-2017     Kukhee Kim   (KHKIM)
               - rebuild against new build system
               

March 22, 2017         Reorganize directory structure, migrate to new build system
